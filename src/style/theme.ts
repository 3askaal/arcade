import { colors, lightColors } from "./colors";
import { fonts } from "./fonts";

export const THEME = {
  rootFontSizes: ['10px', '12px', '14px'],
  fonts,
  colors,
}

export const THEME_LIGHT = {
  rootFontSizes: ['10px', '12px', '14px'],
  fonts,
  colors: lightColors,
}
